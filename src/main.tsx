import React from "react";
import ReactDOM from "react-dom/client";
import Portfolio_index from "./Apps/Portfolio/Portfolio_index";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <Portfolio_index />
  </React.StrictMode>
);
